{
  "name": "manifest",
  "private": true,
  "version": "0.1.0",
  "workspaces": ["web-client"],
  "scripts": {
    "dev": "npm run wasm:build && npm run --workspace=web-client dev",
    "build": "npm run wasm:build && npm run --workspace=web-client build",
    "wasm:build": "cd sim-core && wasm-pack build --target web --out-dir ../web-client/src/wasm",
    "wasm:dev": "cd sim-core && wasm-pack build --target web --dev --out-dir ../web-client/src/wasm"
  }
}
